[workspace]
resolver = "2"
members = ["copy-includes", "desktop", "renderer-core"]

[patch.crates-io]
# TODO: Remove this patch once the next version of wgsl_to_wgpu (with a more granular API that lets us derive this only for vertex buffers)
wgsl_to_wgpu = { git = "https://github.com/ScanMountGoat/wgsl_to_wgpu" }
